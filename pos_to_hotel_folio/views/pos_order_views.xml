<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_pos_order_form_inherit_hotel" model="ir.ui.view">
        <field name="name">pos.order.form.hotel.folio.integration</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <form string="Point of Sale Orders">
                <header>
                    <button name="action_send_to_folio" string="Send to Folio" type="object" class="oe_highlight" invisible="sent_to_folio == True or state != 'draft'"/>
                </header>
                <sheet>
                    <group>
                        <field name="room_id" options="{'no_open': True}" domain="[('id', 'in', occupied_room_ids)]"/>
                        <field name="occupied_room_ids" invisible="1"/>
                        <field name="sent_to_folio" invisible="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>